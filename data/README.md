# Data Files

This directory contains all data files used by SpeedFog, both source files and generated files.

## Source Files (from FogRando)

These files are copied from FogRando and should not be modified:

| File | Description |
|------|-------------|
| `fog.txt` | Zone definitions, fog gates, warps, key items (YAML) |
| `foglocations2.txt` | Enemy areas and scaling tiers (YAML) |
| `er-common.emedf.json` | EMEVD instruction definitions |

## Generated Files

These files are generated by Python tools and can be regenerated:

| File | Generated by | Description |
|------|--------------|-------------|
| `clusters.json` | `tools/generate_clusters.py` | Zone clusters with entry/exit fogs |
| `fog_data.json` | `tools/extract_fog_data.py` | Fog gate metadata for C# writer |

Regenerate with:
```bash
python tools/generate_clusters.py data/fog.txt data/clusters.json --metadata data/zone_metadata.toml
python tools/extract_fog_data.py data/fog.txt data/fog_data.json --validate-clusters data/clusters.json
```

## Configuration Files

| File | Description |
|------|-------------|
| `zone_metadata.toml` | Zone weight defaults and per-zone overrides |
| `speedfog-events.yaml` | EMEVD event templates for the C# writer |

## Runtime Output

The DAG generator outputs files to `output/<seed>/`:
- `graph.json` - Generated DAG consumed by C# writer
- `spoiler.txt` - Human-readable path visualization
